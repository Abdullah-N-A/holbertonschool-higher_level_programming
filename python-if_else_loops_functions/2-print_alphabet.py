#!/usr/bin/python3
import string

# Python's string module provides a constant for the lowercase alphabet.
alphabet = string.ascii_lowercase

# Print the alphabet string on a single line.
# The 'end=""' argument suppresses the default newline character.
print(alphabet, end="")
