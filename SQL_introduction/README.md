SQL Project
List databases: Write a script that lists all databases in your MySQL server.

Create a database: Write a script to create a new database.

Delete a database: Write a script to delete an existing database.

List tables: Write a script that lists all tables of a given database.

First table: Create your first table within a database.

Full description: Show the full description of a table’s schema.

List all in table: Write a query to list all rows of a table.

First add: Insert your first record into a table.

Count 89: Count the number of records with a specific condition (e.g., where a column equals 89).

Full creation: Write the full table creation statement including all columns and constraints.

List by best: List rows ordered by the best scores or values.

Select the best: Select the top record(s) based on certain criteria.

Cheating is bad: Ensure no duplicate or invalid data (focus on data integrity).

Score too low: Select records where scores are below a certain threshold.

Average: Calculate the average value of a numeric column.

Number by score: Group records by score and count how many fall into each group.

Say my name: A fun or final query task, like selecting a specific name or value.
